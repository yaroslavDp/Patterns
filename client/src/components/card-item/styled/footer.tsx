import styled from "@emotion/styled";

import { GRID } from "../../../common/constants";

const Footer = styled.div`
  display: flex;
  margin-top: ${GRID}px;
  align-items: center;
`;

export { Footer };
